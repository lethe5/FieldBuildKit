# Legacy development utility only. The desktop app does not load this file.
# Normal project creation uses explicit local inputs chosen in wizard step 5.
# Never put real credentials in tracked files.
# REFERENCE_DATA_DIR=/path/to/legacy/reference
