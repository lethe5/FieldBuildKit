# Optional local reference data

The full workbook and pinned source manifest are no longer distributed here (0.2.0).
The wizard accepts a local `.xlsx` and an optional local probability-TIFF folder independently.
The only shipped workbook is `resources/samples/taxonomy_sample.xlsx`: three fictional rows
and an instruction sheet. Packaging does not derive or embed private reference datasets.
